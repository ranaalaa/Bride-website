<%= notice %>
<body>
 
  <%= form_tag(photographers_path, :method => "get", class: "navbar-form", id: "search-form") do %>
    <div class="input-append">
      <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search " %>
      <!-- In order to have the "search" icon int the button, we need to use plain HTML instead 
           of using a Rails form helper -->
      <button class="btn"   type="submit" ><i class="icon-search"  > Search </i></button>
      <% end %>
      <% yield %>
    </div>

</body>
Photographers<br>
<% @photographer.each do |photographer| %>
 <%= image_tag photographer.image.url(:small),{} %><br>
     <h1 class="photographer-name"><%= link_to photographer.name, dj %></h1>

<% if user_signed_in? %>
<% if current_user.admin? %>
<%= link_to 'Edit', edit_photographer_path(photographer) %> 
<%= link_to 'Destroy', photographer, method: :delete, data: { confirm: 'Are you sure?' } %><br>
<% end %>
 <% end %>

  <% end %>


<% if user_signed_in? %>
<% if current_user.admin? %>
 <%= link_to 'New Photographer', new_photographer_path %> 
 <% end %>
 <% end %>


