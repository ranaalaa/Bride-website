<%= notice %>
 <body>
 
  <%= form_tag(makeups_path, :method => "get", class: "navbar-form", id: "search-form") do %>
    <div class="input-append">
      <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search " %>
      <!-- In order to have the "search" icon int the button, we need to use plain HTML instead 
           of using a Rails form helper -->
      <button class="btn"   type="submit" ><i class="icon-search"  > Search </i></button>
      <% end %>
      <% yield %>
    </div>

</body>
Listing Make-up Artists <br>
<% @makeup.each do |makeup| %>
 <%= image_tag makeup.image.url(:small),{} %><br>
     <h1 class="makeup-name"><%= link_to makeup.name, makeup %></h1>

<% if user_signed_in? %>
<% if current_user.admin? %>
<%= link_to 'Edit', edit_makeup_path(makeup) %> 
<%= link_to 'Destroy', makeup, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
 <% end %>

  <% end %>


<% if user_signed_in? %>
<% if current_user.admin? %>
 <%= link_to 'New Make-up Artist', new_makeup_path %> 
 <% end %>
 <% end %>


