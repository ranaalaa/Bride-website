<% @vendor.packages.each do |package| %>

 <strong>name:</strong>
    <%=  package.name%><br>
   <strong>price:</strong>
<%=  package.price%><br>
   <strong>description:</strong>

<%=  package.description%><br><br>
<% if user_signed_in? %>
<% if current_user.admin? %>
  <%= link_to 'Delete Package', [package.vendor, package],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
                 <%= link_to 'Edit Package', edit_vendor_package_path(@vendor, package) %> <br><br>
</p>
<% end %>
<% end %>
<% end %>
</p>

<% if user_signed_in? %>
<% if current_user.admin? %>
  <%= form_for([@vendor, @vendor.packages.build]) do |f| %>
  <p>
    <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :price %><br>
    <%= f.text_area :price %>
  </p>
   <p>
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </p>
  <p>
  <%= f.submit %> 
  </p>
<% end %>
<% end %>
<% end %>
